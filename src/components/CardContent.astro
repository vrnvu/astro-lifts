---
interface Props {
  title: string;
  detail?: string;
}

const { title, detail} = Astro.props;
---

<div class="w-full flex justify-between text-[12px] leading-tight">
  <p class="w-fit text-[#555]">{title}</p>
  {detail && <p class="w-fit text-[#555] hidden md:inline">{detail}</p>}
</div>
